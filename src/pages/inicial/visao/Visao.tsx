
import { Box, Typography } from '@mui/material';

export const Visao: React.FC = () => {
  return (
    <Box height='70vh' sx={{ backgroundColor: 'background.paper' }}>
      <Typography>
            Visão
      </Typography>
    </Box>
  );
};
